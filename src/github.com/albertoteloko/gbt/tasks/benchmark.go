package tasks

import (
	pd "github.com/albertoteloko/gbt/project-definition"
)

func benchmark(pd pd.ProjectDefinition) error {
	//os.RemoveAll(BIN_FOLDER)
	//os.MkdirAll(BIN_FOLDER, 0777)

	return nil
}
